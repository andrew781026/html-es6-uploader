{
  "name": "uploader",
  "version": "0.0.1",
  "description": "make the image upload block",
  "scripts": {
    "serve": "browser-sync start --server --files \"*.html, css/*.css, js/*.js\""
  },
  "author": "andrew",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "gulp": "^4.0.2"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "nodemon": "^2.0.3"
  }
}
